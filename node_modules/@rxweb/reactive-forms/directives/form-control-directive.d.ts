import { RxFormControl } from "../services/form-control";
export declare class FormControlDirective {
    private element;
    private formControl;
    _onChange: (e: any) => void;
    _onBlur: (e: any) => void;
    constructor(element: HTMLInputElement, formControl: RxFormControl);
    private bindEvent;
    private onChange;
    private onBlur;
    private setValueOnControl;
    private setValue;
    subscribeOnChange(): void;
    destroy(): void;
}
