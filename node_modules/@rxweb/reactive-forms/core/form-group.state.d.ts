import { RxFormGroup } from "../services/rx-form-group";
export declare const formGroupContainer: {
    get(groupKey: string): RxFormGroup;
    saveFormGroup(formGroup: RxFormGroup): any;
    mapElement(groupKey: string, element: HTMLElement): any;
    destroy(groupKey: string): any;
};
