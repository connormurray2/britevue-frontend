import { AbstractControl } from '../abstract/abstract-control';
export declare function timeChecker(control: AbstractControl, config: any, operationType: string): {
    [key: string]: any;
} | null;
